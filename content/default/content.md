# TRMNL Generic Display

Nessun contenuto pianificato per questo momento.

Per aggiungerne uno:

1. crea una directory in `/content`
2. aggiungi il file del contenuto
3. crea `meta.json` con `id`, `type`, `source`, `start`
4. commit e push

Lo `schedule.json` viene rigenerato automaticamente dalla build.
